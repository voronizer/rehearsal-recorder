# Packaging output
build/
dist/
*.spec.bak

# Python
__pycache__/
*.py[cod]
venv/
.venv/
*.egg-info/
src/*.egg-info/

# ui/dist is build output and is not committed — the build scripts and CI
# both produce it. See docs/development.md.
ui/node_modules/

.DS_Store

# Personal Claude Code settings for this checkout, not the project's.
.claude/settings.local.json

# Screenshots the interface suite takes as it runs
tests/screenshots/

# Written by setuptools-scm from the git tag at build time.
src/rehearsal_recorder/_version.py
